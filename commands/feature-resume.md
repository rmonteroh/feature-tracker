---
description: Resume a paused feature (defaults to one in the current project)
argument-hint: [name text, optional]
allowed-tools: Bash
---

Run this command exactly and show its output without adding commentary:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/feature-resume.sh" "$ARGUMENTS"
```
