---
description: Regenerate all feature .md files in $FEATURE_TRACKER_MD_EXPORT_DIR from the log
allowed-tools: Bash
---

Run this command exactly and show its output without adding commentary:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/feature-export-md.sh"
```
