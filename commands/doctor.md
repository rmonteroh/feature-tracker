---
description: Diagnose plugin config, dependencies, and state — show what's set, what's missing, and any errors
allowed-tools: Bash
---

Run this command exactly and show its output without adding commentary:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/feature-doctor.sh"
```
