---
description: List all paused features, grouped by current project vs. others
allowed-tools: Bash
---

Run this command exactly and show its output without adding commentary:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/feature-list-paused.sh"
```
