---
description: Show stats of logged features (total, by project, by day)
allowed-tools: Bash
---

Run this command exactly and show its output without adding commentary:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/feature-stats.sh"
```
