---
description: Close the in-progress feature, compute its duration, and append it to the log
allowed-tools: Bash
---

Run this command exactly and show its output without adding commentary:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/bin/feature-done.sh"
```
